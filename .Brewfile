# Homebrew is the "missing package manager for macOS".  Read more here: https://brew.sh/
#
# This is an example Brewfile.  For brew packages installed with `brew install Caskroom/cask/{package}`,
# use the `cask "package-name"` syntax.  For brew packages installed with `brew install {package}`,
# use the `brew "package-name"` syntax.
# Explore packages available by browsing to http://brewformulas.org/.
# 
# Use this script to customize your Strap setup with packages you like!

# Some examples:
# Tools
cask "kdiff3"

# Misc
cask "dropbox"
